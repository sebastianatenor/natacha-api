# ops/timeline/writer.py
def write_event(*args, **kwargs):
    """
    Stub seguro.
    B5 proposal-only: no escribe eventos reales.
    """
    return {
        "status": "noop",
        "reason": "timeline writer not active (proposal-only)",
    }

