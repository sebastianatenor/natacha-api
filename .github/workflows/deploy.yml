name: 🚀 Natacha CI/CD

on:
  push:
    branches:
      - main
    paths:
      - "natacha_base/**"
      - "routes/**"
      - "memory_console/**"
      - "natacha_core/**"
      - "Dockerfile"
      - "docker-compose.yml"
      - ".github/workflows/deploy.yml"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      PROJECT_ID: asistente-sebastian
      REGION: us-central1
      MEMORY_SERVICE: natacha-memory-console
      CORE_SERVICE: natacha-core
      API_SERVICE: natacha-api

    steps:
      - name: 🧩 Checkout repo
        uses: actions/checkout@v4

      # === Debug: Verificar que el secret exista ===
      - name: 🧪 Test GCP secret (debug)
        run: |
          echo "🔍 Probando si el secret GCP_SERVICE_ACCOUNT_KEY llega desde GitHub..."
          if [ -z "${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}" ]; then
            echo "❌ El secret GCP_SERVICE_ACCOUNT_KEY está VACÍO o no está definido."
            exit 1
          else
            echo "✅ El secret GCP_SERVICE_ACCOUNT_KEY está presente."
          fi

      # === Autenticación con Google Cloud ===
      - name: 🔐 Auth with Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}
          project_id: asistente-sebastian

      # === Docker ===
      - name: 🐳 Setup Docker
        uses: docker/setup-buildx-action@v3

      - name: 🏗️ Build and Push Containers
        run: |
          echo "Building and pushing services..."
          for SERVICE in $MEMORY_SERVICE $CORE_SERVICE $API_SERVICE; do
            echo "🧱 Building $SERVICE..."
            docker build -t gcr.io/$PROJECT_ID/$SERVICE:latest -f $SERVICE/Dockerfile .
            docker push gcr.io/$PROJECT_ID/$SERVICE:latest
          done

      # === Deploy ===
      - name: 🚀 Deploy to Cloud Run
        run: |
          echo "Deploying services to Cloud Run..."
          for SERVICE in $MEMORY_SERVICE $CORE_SERVICE $API_SERVICE; do
            echo "📦 Deploying $SERVICE..."
            gcloud run deploy $SERVICE \
              --image gcr.io/$PROJECT_ID/$SERVICE:latest \
              --region $REGION --platform managed --allow-unauthenticated
          done

      # === Health check final ===
      - name: ✅ Health Check
        run: |
          echo "Checking services..."
          curl -f https://${{ env.API_SERVICE }}-${{ env.REGION }}.a.run.app/health || exit 1
