#!/usr/bin/env bash
set -euo pipefail

if [[ -f REGISTRY.md ]]; then
  COUNT="$(grep -E '^### (🧩|<0001f9e9>) OpenAPI Schema' -c REGISTRY.md || true)"
  if [[ "${COUNT:-0}" -gt 1 ]]; then
    echo "❌ Hay más de un bloque 'OpenAPI Schema' en REGISTRY.md. Ejecutá scripts/update_registry_openapi.sh y reintenta."
    exit 1
  fi
fi
exit 0
